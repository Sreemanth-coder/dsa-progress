#include<bits/stdc++.h>
using namespace std;

void peak(vector<vector<int>>&mat){
  for(int i=0;i<mat.size();i++){
    for(int j=0;j<mat[i].size();j++){
      int num=mat[i][j];
      while(j<mat[i].size()){
        
      }
    }
  }

}

int main(){
  vector<vector<int>> mat={{1,4},{3,2}};
  peak(mat);
}